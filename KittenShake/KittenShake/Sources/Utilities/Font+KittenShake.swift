import SwiftUI

extension Font {
    /// The playful display font used for titles throughout the app.
    static func freckleFace(size: CGFloat) -> Font {
        .custom("FreckleFace-Regular", size: size)
    }
}
